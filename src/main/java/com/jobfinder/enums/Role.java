package com.jobfinder.enums;

// Defines user authorization roles within the job finder platform
public enum Role {
    // Standard job seeker user who can search jobs and submit applications
    JOB_SEEKER,
    // Recruiter user who can post jobs and review applications
    RECRUITER,
    // Administrator user who has access to platform management and moderation tools
    ADMIN
}
