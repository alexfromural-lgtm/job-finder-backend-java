package com.jobfinder.enums;

// Defines the types of notifications sent to users
public enum NotificationType {
    // Notification regarding an update on a job application (e.g. status change)
    application_update,
    // Notification for system-wide announcements or administration updates
    system
}
